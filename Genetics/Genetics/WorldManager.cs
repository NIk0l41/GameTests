using System;
using System.Collections.Generic;
using System.Text;

namespace Genetics
{
    class WorldManager
    {
        int numOfBacteria;

        void Start()
        {
            numOfBacteria = 2;
            GenerateBacteria(numOfBacteria);
        }

        void GenerateBacteria(int a) {
            for (int i = 0; i < a; i++) {
                
            }
        }
    }
}
